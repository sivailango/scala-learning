package com.aurum.scala.basics

object DataTypes {

	def main(args: Array[String]) {
		var message = "Hello Wolrd from Scala!";
		println(message);
		var (x, y) = (10, 20);
		println(s"x = $x \t y = $y")
	}	
}